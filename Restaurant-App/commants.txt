npm create vite@latest . -- --template react
npm install

npm install tailwindcss@latest @tailwindcss/vite@latest

import tailwindcss from '@tailwindcss/vite'
tailwindcss()

@import "tailwindcss";

********
styling 


/* 2. Слой Base — глобальные стили для тегов */
/* @layer base {
  body {
    @apply font-poppins bg-gray-50 text-gray-900;
  }
  h1, h2, h3 {
    @apply font-averia font-bold;
  }
  a {
    @apply transition-colors hover:text-primary;
  }
} */

/* 3. Слой Components — твои переиспользуемые классы */
/* @layer components {
  .fruit-card {
    @apply bg-white p-4 rounded-3xl shadow-md border border-gray-100 
           hover:shadow-xl transition-all duration-300;
  }
  
  .badge-hot {
    @apply bg-primary text-white text-xs px-2 py-1 rounded-full uppercase;
  }
} */

/* 4. Слой Utilities — если нужно что-то совсем кастомное */
/* @layer utilities {
  .text-shadow-red {
    text-shadow: 2px 2px 4px rgba(222, 44, 77, 0.3);
  }
} */